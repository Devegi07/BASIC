name: Simple Dual Error Test

on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: finding error
        run: |
          echoo "Starting test"
          pythno3 --versoin
